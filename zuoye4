size(600,400);
background(255);
String txt ="Happiness is a butterfly";
float ww = 0;
for (int i = 0; i < txt.length(); i++) {
char c = txt.charAt(i);
textSize(floor(random(36,100)));
int cl = int(c);
println(cl);
fill(cl, 100);
noStroke();
rect(ww, 150, 50, 50);

ww += textWidth(c);
}
